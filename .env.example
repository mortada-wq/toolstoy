# Billing provider: 'stripe' | 'aws_marketplace'
# VITE_BILLING_PROVIDER=stripe

# When using Stripe: URL for Pro checkout (create Stripe Checkout session server-side)
# VITE_STRIPE_CHECKOUT_URL=https://checkout.stripe.com/...

# When using Stripe: customer portal for managing billing
# VITE_STRIPE_PORTAL_URL=https://billing.stripe.com/...
